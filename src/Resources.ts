import Scene from "./Scene";

const Resource = Object.freeze({
  Title: {
    Bg: "Ukraine.png"
  },
  Map: "provinces.bmp",
  FontFamily: {
    Default: "MisakiGothic"
  }
});

export default Resource;
